using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HolaMundo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() //Se invoca cuando inicia el videojuego, como si fuera una clase de constructor
    {
        Debug.Log("Hola Mundo...");
    }

    // Update is called once per frame
    void Update() //se ejecuta por cada frame (actualización de la pantalla según el diapositivo) del videojuego 
    {

    }
}
